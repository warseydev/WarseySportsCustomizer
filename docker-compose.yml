version: "3.9"
services:
  warsey-customizer:
    restart: always
    build: .
    ports:
      - "7000:7000"
    volumes:
      - ~/warseyapps-files/warseycustomizer:/usr/share/warseyapifrontend/db/:ro